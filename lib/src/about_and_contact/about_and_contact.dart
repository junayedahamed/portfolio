import 'package:flutter/material.dart';

class AboutAndContact extends StatelessWidget {
  const AboutAndContact({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
